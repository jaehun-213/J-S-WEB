# Firebase 문제 해결 가이드

이 문서는 견적 요청 시스템의 일반적인 문제를 해결하는 방법을 안내합니다.

## 🔍 문제 진단

### 1단계: Firebase 연결 테스트

`firebase-debug.html` 페이지를 열어 Firebase 연결 상태를 확인하세요.
- 브라우저에서 `firebase-debug.html` 파일을 엽니다
- 각 테스트 버튼을 클릭하여 연결 상태를 확인합니다

### 2단계: 브라우저 콘솔 확인

브라우저 개발자 도구(F12)를 열고 콘솔 탭에서 오류 메시지를 확인하세요.

---

## ❌ 문제 1: 견적 요청이 업데이트되지 않음

### 가능한 원인 및 해결 방법

#### 원인 1: Firestore 데이터베이스가 생성되지 않음

**해결 방법:**
1. Firebase 콘솔 접속: https://console.firebase.google.com/project/pythondb-e737a
2. 왼쪽 메뉴에서 "Firestore Database" 클릭
3. "데이터베이스 만들기" 버튼 클릭
4. "프로덕션 모드에서 시작" 선택
5. 위치 선택 (예: asia-northeast3 (Seoul))
6. "설정" 클릭

#### 원인 2: Firestore 보안 규칙이 잘못 설정됨

**해결 방법:**
1. Firebase 콘솔 > Firestore Database > 규칙 탭
2. 다음 규칙을 적용:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /quoteRequests_public/{docId} {
      allow read: if true;
      allow write: if true;
    }
    match /quoteRequests_private/{docId} {
      allow read, update, delete: if request.auth != null;
      allow create: if true;
    }
  }
}
```

3. "게시" 버튼 클릭

#### 원인 3: Firebase Storage가 생성되지 않음

**해결 방법:**
1. Firebase 콘솔 > Storage 메뉴
2. "시작하기" 클릭
3. 보안 규칙 설정 (기본 규칙 사용 가능)
4. 위치 선택 (Firestore와 동일한 위치 권장)

---

## ❌ 문제 2: 관리자 로그인이 안 됨

### 가능한 원인 및 해결 방법

#### 원인 1: Firebase Authentication이 활성화되지 않음

**해결 방법:**
1. Firebase 콘솔 > Authentication 메뉴
2. "시작하기" 클릭
3. "로그인 방법" 탭 클릭
4. "이메일/비밀번호" 클릭
5. "사용 설정" 토글을 활성화
6. "저장" 클릭

#### 원인 2: 관리자 계정이 생성되지 않음

**해결 방법:**
1. Firebase 콘솔 > Authentication > 사용자 탭
2. "사용자 추가" 버튼 클릭
3. 이메일과 비밀번호 입력
4. "사용자 추가" 클릭

**⚠️ 중요:** 이메일과 비밀번호를 정확히 입력하세요. 대소문자를 구분합니다.

#### 원인 3: 오타 또는 잘못된 계정 정보

**해결 방법:**
- 브라우저 콘솔에서 오류 메시지 확인
- 오류 코드에 따라:
  - `auth/user-not-found`: 이메일이 등록되지 않음 → 계정 생성 필요
  - `auth/wrong-password`: 비밀번호가 틀림 → 비밀번호 재확인
  - `auth/invalid-email`: 이메일 형식 오류 → 이메일 형식 확인

---

## ❌ 문제 3: 견적 목록이 표시되지 않음

### 가능한 원인 및 해결 방법

#### 원인 1: Firestore 인덱스가 필요함

**해결 방법:**
1. 브라우저 콘솔에서 오류 확인
2. 오류 메시지에 인덱스 생성 링크가 포함되어 있으면 클릭
3. 또는 Firebase 콘솔 > Firestore Database > 인덱스 탭에서 수동 생성

**인덱스 생성 정보:**
- 컬렉션: `quoteRequests_public`
- 정렬 필드: `createdAt` (내림차순)

#### 원인 2: 데이터가 실제로 없음

**확인 방법:**
1. Firebase 콘솔 > Firestore Database > 데이터 탭
2. `quoteRequests_public` 컬렉션 확인
3. 데이터가 없으면 견적 요청을 다시 제출해보세요

---

## 🛠️ 단계별 해결 절차

### 1단계: Firebase 서비스 확인

1. **Firestore Database 생성 확인**
   - Firebase 콘솔에서 Firestore Database가 생성되어 있는지 확인
   - 없으면 생성

2. **Firebase Storage 생성 확인**
   - Firebase 콘솔에서 Storage가 생성되어 있는지 확인
   - 없으면 생성

3. **Firebase Authentication 활성화 확인**
   - Firebase 콘솔에서 Authentication > 로그인 방법에서 이메일/비밀번호 활성화 확인

### 2단계: 보안 규칙 확인

1. **Firestore 보안 규칙**
   - 위의 규칙 코드를 복사하여 적용
   - "게시" 버튼 클릭 필수

2. **Storage 보안 규칙**
   - Storage > 규칙 탭에서 규칙 확인
   - 필요시 업데이트

### 3단계: 테스트

1. `firebase-debug.html` 페이지에서 각 테스트 실행
2. 브라우저 콘솔에서 오류 메시지 확인
3. 오류 메시지에 따라 조치

---

## 📞 추가 도움말

문제가 계속되면 다음 정보를 확인하세요:

1. **브라우저 콘솔 오류 메시지** (F12 > Console)
2. **Firebase 콘솔의 서비스 상태**
3. **네트워크 연결 상태**

Firebase 공식 문서: https://firebase.google.com/docs


